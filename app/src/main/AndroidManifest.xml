<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quang.timeslots">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".TimeSlotsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TimeSlotsTheme">
        <activity android:name=".habitlist.HabitListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".habitdetails.HabitDetailsActivity"
            android:label="@string/title_activity_habit_details"
            android:launchMode="singleTask"
            android:parentActivityName=".habitlist.HabitListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".habitlist.HabitListActivity" />
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".habitlist.HabitListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".habitlist.HabitListActivity" />
        </activity>

        <receiver android:name=".common.NotificationBroadcastReceiver" />
    </application>

</manifest>